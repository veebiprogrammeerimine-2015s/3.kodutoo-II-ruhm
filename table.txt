CREATE TABLE book_list (
  id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  user_name NOT NULL,
  book_name VARCHAR(30),   
  book_description VARCHAR(30),
  book_rating VARCHAR(3),
  FOREIGN KEY (user_id) REFERENCES user_sample(id)
);